@{
    var isLoggedIn = Session["AuthUserID"] != null;
    var fullName = Session["AuthFullName"] as string;
    var roleName = Session["AuthRoleName"] as string;
    var currentController = ViewContext.RouteData.Values["controller"] as string;
    var currentAction = ViewContext.RouteData.Values["action"] as string;
    var isBackOfficeUser = string.Equals(roleName, "Admin", System.StringComparison.OrdinalIgnoreCase)
        || string.Equals(roleName, "HR", System.StringComparison.OrdinalIgnoreCase);
    var isCandidate = string.Equals(roleName, "Candidate", System.StringComparison.OrdinalIgnoreCase);
    var avatarVersion = Session["AuthAvatarVersion"] ?? 0;
    var avatarInitial = (string.IsNullOrWhiteSpace(fullName) ? "U" : fullName.Substring(0, 1)).ToUpperInvariant();
    var displayRole = isCandidate ? "Ứng viên"
        : string.Equals(roleName, "HR", System.StringComparison.OrdinalIgnoreCase) ? "Nhân sự"
        : string.Equals(roleName, "Admin", System.StringComparison.OrdinalIgnoreCase) ? "Quản trị viên"
        : roleName;
    Func<string, string, string> activeClass = (controller, action) =>
        string.Equals(currentController, controller, System.StringComparison.OrdinalIgnoreCase) &&
        (string.IsNullOrEmpty(action) || string.Equals(currentAction, action, System.StringComparison.OrdinalIgnoreCase))
            ? "active"
            : string.Empty;
}

<nav class="navbar navbar-expand-lg public-navbar sticky-top">
    <div class="container">
        <a class="navbar-brand public-brand" href="@Url.Action("Index", "Home")" aria-label="ATS Mini - Trang chủ">
            <img class="public-brand-logo"
                 src="@Url.Content("~/Content/images/brand/ats-mini-wordmark.png")"
                 alt=""
                 width="850"
                 height="261" />
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#publicNavbar" aria-controls="publicNavbar" aria-expanded="false" aria-label="Mở menu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="publicNavbar">
            <ul class="navbar-nav mx-auto mb-2 mb-lg-0 public-nav-list">
                <li class="nav-item"><a class="nav-link @activeClass("Home", "Index")" href="@Url.Action("Index", "Home")">Trang chủ</a></li>
                <li class="nav-item"><a class="nav-link @activeClass("Home", "About")" href="@Url.Action("About", "Home")">Về ATS Mini</a></li>
                <li class="nav-item"><a class="nav-link @activeClass("Jobs", "")" href="@Url.Action("Index", "Jobs")">Việc làm</a></li>
                @if (!isLoggedIn)
                {
                    <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Home")#huong-dan-ung-tuyen">Hướng dẫn ứng tuyển</a></li>
                }
                @if (isCandidate)
                {
                    <li class="nav-item"><a class="nav-link @activeClass("Candidate", "Index")" href="@Url.Action("Index", "Candidate")">Tổng quan</a></li>
                }
                @if (isBackOfficeUser)
                {
                    <li class="nav-item"><a class="nav-link @activeClass("Dashboard", "Index")" href="@Url.Action("Index", "Dashboard")">Quản trị tuyển dụng</a></li>
                }
            </ul>
            <div class="public-nav-actions @(isCandidate ? "has-candidate-account-menu" : "")">
                @if (isLoggedIn)
                {
                    if (isCandidate)
                    {
                        <details class="candidate-account-menu">
                            <summary class="candidate-user candidate-user-trigger" aria-label="Mở menu tài khoản của @fullName">
                                <span class="candidate-user-avatar">
                                    <span aria-hidden="true">@avatarInitial</span>
                                    <img src="@Url.Action("Avatar", "Candidate", new { v = avatarVersion })"
                                         alt=""
                                         data-candidate-avatar-image />
                                </span>
                                <span class="candidate-user-meta">
                                    <strong>@fullName</strong>
                                    <small>@displayRole</small>
                                </span>
                                <svg class="candidate-account-chevron" viewBox="0 0 24 24" aria-hidden="true">
                                    <path d="m8 10 4 4 4-4"></path>
                                </svg>
                            </summary>

                            <div class="candidate-account-popover" role="menu">
                                <div class="candidate-account-heading">
                                    <strong>@fullName</strong>
                                    <span>@displayRole</span>
                                </div>
                                <a class="candidate-account-menu-item @activeClass("Applications", "")"
                                   href="@Url.Action("Status", "Applications")"
                                   role="menuitem">
                                    <svg viewBox="0 0 24 24" aria-hidden="true">
                                        <path d="M7 4h10v4H7z"></path>
                                        <path d="M5 6h14v14H5zM8 12h8M8 16h5"></path>
                                    </svg>
                                    <span>
                                        <strong>Hồ sơ ứng tuyển</strong>
                                        <small>Theo dõi trạng thái ứng tuyển</small>
                                    </span>
                                </a>
                                <a class="candidate-account-menu-item @activeClass("Candidate", "SavedJobs")"
                                   href="@Url.Action("SavedJobs", "Candidate")"
                                   role="menuitem">
                                    <svg viewBox="0 0 24 24" aria-hidden="true">
                                        <path d="M6 4h12v17l-6-4-6 4z"></path>
                                    </svg>
                                    <span>
                                        <strong>Tin đã lưu</strong>
                                        <small>Xem lại các cơ hội bạn quan tâm</small>
                                    </span>
                                </a>
                                <a class="candidate-account-menu-item @activeClass("Candidate", "Profile")"
                                   href="@Url.Action("Profile", "Candidate")"
                                   role="menuitem">
                                    <svg viewBox="0 0 24 24" aria-hidden="true">
                                        <circle cx="12" cy="8" r="4"></circle>
                                        <path d="M5 20a7 7 0 0 1 14 0"></path>
                                    </svg>
                                    <span>
                                        <strong>Hồ sơ cá nhân</strong>
                                        <small>Cập nhật thông tin và CV</small>
                                    </span>
                                </a>
                                @using (Html.BeginForm("Logout", "Account", FormMethod.Post, new { @class = "candidate-account-logout" }))
                                {
                                    @Html.AntiForgeryToken()
                                    <button class="candidate-account-menu-item" type="submit" role="menuitem">
                                        <svg viewBox="0 0 24 24" aria-hidden="true">
                                            <path d="M10 5H6a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h4"></path>
                                            <path d="m15 8 4 4-4 4M9 12h10"></path>
                                        </svg>
                                        <span>
                                            <strong>Đăng xuất</strong>
                                            <small>Kết thúc phiên làm việc</small>
                                        </span>
                                    </button>
                                }
                            </div>
                        </details>
                    }
                    else
                    {
                        <div class="candidate-user">
                            <span class="candidate-user-avatar">
                                <span aria-hidden="true">@avatarInitial</span>
                            </span>
                            <span>
                                <strong>@fullName</strong>
                                <small>@displayRole</small>
                            </span>
                        </div>
                        using (Html.BeginForm("Logout", "Account", FormMethod.Post, new { @class = "d-inline" }))
                        {
                            @Html.AntiForgeryToken()
                            <button class="btn btn-outline-secondary btn-sm" type="submit">Đăng xuất</button>
                        }
                    }
                }
                else
                {
                    <a class="btn btn-link btn-sm public-login-link text-decoration-none fw-semibold" href="@Url.Action("Login", "Account")">Đăng nhập</a>
                    <a class="btn btn-primary btn-sm fw-semibold px-3" href="@Url.Action("Register", "Account")">Tạo hồ sơ</a>
                }
            </div>
        </div>
    </div>
</nav>
